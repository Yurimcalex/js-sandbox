alert('Something importand is writing here!');

// may be that what 
// users entered or
// null if they canceled 
// or default value if they pressed ok
let favouriteDrink = prompt('What is your favouite drink?', 'whater');
console.log(favouriteDrink);

// the result is true if Ok pressed otherwise false
let isLikeApples = confirm('Do you like apples?');
console.log(isLikeApples);

let userName = prompt('What is your name?', 'Guest');
alert(userName);